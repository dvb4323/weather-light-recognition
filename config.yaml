# Configuration for Weather and Time-of-Day Multi-Task Classification

data:
  root: "data"
  train_images: "data/train_224/img"
  train_anns: "data/train/ann"
  val_images: "data/val_224/img"
  val_anns: "data/val/ann"
  test_images: "data/test/img"  # Original val set (has labels)
  test_anns: "data/test/ann"

classes:
  weather:
    - "clear"
    - "partly cloudy"
    - "overcast"
    - "rainy"
    - "snowy"
  timeofday:
    - "daytime"
    - "dawn/dusk"
    - "night"

model:
  backbone: "efficientnet_b0"
  pretrained: true
  num_weather_classes: 5  # Removed foggy
  num_time_classes: 3     # Including undefined

train:
  batch_size: 32
  num_epochs: 20
  lr: 0.00001
  weight_decay: 0.001
  device: "cuda"  # or "cpu"
  checkpoint_dir: "checkpoints"
  save_freq: 5
  # Class weights for down-weighting undefined (Option 2)
  weather_undefined_weight: 0.01
  time_undefined_weight: 0.01

augmentation:
  input_size: [224, 224]
  mean: [0.485, 0.456, 0.406]
  std: [0.229, 0.224, 0.225]
